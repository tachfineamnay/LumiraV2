name: C1 diagnostics

on:
  push:
    branches: [ launch/c1-core-onboarding ]
  pull_request:
    branches: [ main ]

jobs:
  runner:
    name: runner-smoke-test
    runs-on: ubuntu-latest
    steps:
      - name: Verify runner
        run: echo "GitHub Actions runner is available"
