<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body {
      background-color: #0a0a2e;
      color: #ffffff;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 40px;
      background: linear-gradient(135deg, #1a1a4a 0%, #0a0a2e 100%);
      border: 1px solid #d4af37;
      border-radius: 10px;
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
    }
    .header h1 {
      color: #d4af37;
      font-size: 24px;
    }
    .order-details {
      background: rgba(255, 255, 255, 0.05);
      padding: 20px;
      border-radius: 8px;
      margin: 20px 0;
    }
    .button {
      display: inline-block;
      padding: 12px 24px;
      background: #d4af37;
      color: #0a0a2e;
      text-decoration: none;
      border-radius: 5px;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>🔔 Nouvelle Commande à Valider</h1>
    </div>
    <div class="content">
      <p>Une nouvelle commande nécessite votre expertise spirituelle.</p>
      
      <div class="order-details">
        <p><strong>Commande :</strong> {{orderNumber}}</p>
        <p><strong>Client :</strong> {{clientName}}</p>
        <p><strong>Niveau :</strong> {{level}}</p>
        <p><strong>Reçue le :</strong> {{createdAt}}</p>
      </div>
      
      <p style="text-align: center;">
        <a href="{{adminUrl}}" class="button">Accéder au Dashboard</a>
      </p>
    </div>
  </div>
</body>
</html>
